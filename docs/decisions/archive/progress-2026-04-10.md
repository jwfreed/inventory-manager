Status: Archived. This document is not canonical.

Sprint 2 Phase B Progress Tracker
=================================

Date: 2026-04-10
Scope: QC separation with behavior-preserving characterization, workflow extraction, transfer-boundary clarification, and warehouse-disposition audit fix.

Constraints
- Preserve correctness before coupling reduction.
- Preserve replay semantics.
- Preserve existing transaction boundaries unless a targeted correctness fix is required.
- Do not redesign receipt_allocations ownership in this phase.

Workstream Status
- [completed] Step 1: Create scaffold files and inspect existing QC code/tests.
- [completed] Step 2: Add or confirm characterization coverage for receipt QC behavior.
- [completed] Step 2: Add or confirm characterization coverage for work order QC behavior.
- [completed] Step 2: Add or confirm characterization coverage for execution line QC behavior.
- [completed] Step 2: Add or confirm characterization coverage for warehouse disposition behavior.
- [completed] Step 3: Split createQcEvent into explicit workflow functions.
- [completed] Step 4: Standardize QC to transfer integration boundary.
- [completed] Step 5: Preserve receipt QC side-effect ordering exactly.
- [completed] Step 6: Fix warehouse disposition audit transaction gap.
- [completed] Step 7: Verify replay and idempotency semantics.
- [completed] Step 8: Run affected suites and finalize review.

Current Notes
- Phase A report treated as canonical source of truth.
- Receipt QC remains the highest-risk path because it mutates receipt_allocations and receipt lifecycle state in addition to the ledger-linked transfer.
- `createQcEvent()` now dispatches to explicit receipt, work-order, and execution-line workflow functions with separate prepare/execute/replay paths.
- QC-triggered transfers now run through a single explicit internal transfer boundary in the QC service helpers.
- Receipt QC still preserves `moveReceiptAllocationsFromQa()`, `evaluateQcCommand()`, NCR creation, and existing side-effect ordering.
- Warehouse disposition audit now records inside the transfer transaction and keyed replay no longer writes a duplicate `qc_accept` audit row.
- Verification results:
  - Targeted QC workflow contract file passed.
  - `npm run test:truth` passed.
  - `npm run test:contracts` passed.
  - `npm run lint:inventory-writes` failed on pre-existing ownership violations in migrations, `inventoryBalance.projector.ts`, and `inventoryLedgerReconcile.service.ts`.
  - `npm run test:scenarios` surfaced `multi-warehouse load: no oversell, warehouse isolation, bounded retries, strict invariants clean` as a failing existing scenario outside the QC files changed here.
- receipt_allocations ownership remains unresolved and must stay unchanged in this phase.

Sprint 2 Phase C Progress Tracker (Explicit Transaction Boundary Refactor)
==========================================================================

Date: 2026-04-10
Scope: Replace callback-based `transactionalSideEffect` in `transferInventory` with an explicit transaction boundary model.

Migration to explicit transaction model:
- Introduced `withInventoryTransaction<T>` in `src/modules/platform/application/withInventoryTransaction.ts`.
  Uses `withTransactionRetry` with SERIALIZABLE isolation and retry=2, matching `runInventoryCommand` semantics.
- Removed `transactionalSideEffect` callback from `TransferInventoryInput` type.
- Added `TransferInventoryOptions = { client: PoolClient }` to `transfers.service.ts`.
- Added `executeTransferWithExternalClient` private helper that handles idempotency claim/finalize, lock acquisition, transfer execution, event appending, and projection ops using a caller-provided client.
- `transferInventory(input, options?)` now accepts an explicit client via `options.client`; with-client path uses `executeTransferWithExternalClient`. Without-client path is unchanged (`runInventoryCommand`).
- `postQcWarehouseDisposition` now wraps both `transferInventory` and `recordAuditLog` inside `withInventoryTransaction`, making transfer + audit atomic.
- Audit write skipped on replay via `if (!t.replayed)` guard.

Invariants preserved:
- receipt_allocations logic unchanged; createQcEvent workflows unchanged.
- Work Order QC null bypass intact.
- Execution Line quantity cap intact.
- Replay semantics preserved: onReplay in both with-client and runInventoryCommand paths reconstruct using same deterministic hash logic.
- Idempotency preserved: claimTransactionalIdempotency and finalizeTransactionalIdempotency called identically in with-client path.
- No ledger writes outside ledgerWriter.ts.
- Existing architecture tests continue to pass.

New test:
- `warehouse disposition transfer and audit log are co-committed within the same explicit transaction boundary` in `tests/contracts/qc-workflow-boundaries.test.mjs`.
  Proves: movement and audit row co-exist after single committed call; replay does not insert a second audit row.

Sprint 2 Phase D Progress Tracker (Transfer Orchestration Unification)
=====================================================================

Date: 2026-04-12
Scope: Unify standalone and external-client transfer execution into one shared orchestration path with behavior-preserving parity coverage.

Implementation:
- Added one internal transfer executor: `executeTransferWithClient(client, params)`.
- Removed `executeTransferWithExternalClient` as a separate orchestration implementation.
- `transferInventory(input, { client })` now calls `executeTransferWithClient(...)` directly.
- The standalone `transferInventory(input)` path keeps the `runInventoryCommand` transaction/retry shell but delegates transfer orchestration to `executeTransferWithClient(...)`.
- The single transfer executor owns the sequence: idempotency claim, replay handling, ATP lock acquisition, transfer execution, inventory event append, projection ops, and idempotency finalize.

Parity guarantees:
- Contract coverage now compares persisted movement rows, movement lines, inventory events, inventory balance projection rows, idempotency rows, and replay write counts across standalone and external-client paths.
- Replay parity verifies both paths return replayed responses and create zero additional movement, line, event, or idempotency rows.
- External-client rollback coverage verifies a failure after transfer execution and transactional audit write commits no movement, no audit, and no idempotency row; retry creates exactly one movement.
- Ordering coverage verifies the shared executor source order and persisted event/projection/finalize state match across both paths.

Invariant status:
- No receipt_allocations behavior changed.
- Work Order QC NULL bypass unchanged.
- Execution Line QC cap unchanged.
- QC workflow structure unchanged.
- No schema or migration changes.
- No known invariant drift introduced.

Known limitations:
- Parity tests normalize tenant-specific IDs and deterministic hash values when comparing two independent executions; they still assert persisted row counts, stable semantic values, and hash format.

Sprint 2 Phase E Progress Tracker (Transfer Boundary Enforcement)
=================================================================

Date: 2026-04-12
Scope: Remove the generic caller-client execution bypass from `runInventoryCommand` and make the caller-client transfer path structurally transfer-scoped.

Implementation:
- Removed `executeWithClient` from `runInventoryCommand`; it has one lifecycle model again.
- Added private `runTransferCommandWithClient(...)` inside `src/services/transfers.service.ts`.
- Standalone `transferInventory(input)` uses the private transfer-scoped runner for SERIALIZABLE retry=2 transaction ownership.
- External-client `transferInventory(input, { client })` still calls `executeTransferWithClient(...)` inside the caller-owned transaction.
- `runTransferCommandWithClient(...)` is not exported from transfer service or shared platform infrastructure.

Invariant enforcement:
- Both standalone and external-client transfer paths still use exactly one transfer orchestration implementation: `executeTransferWithClient(...)`.
- Transfer lifecycle order remains: claim/replay, lock, execute, append events, projection ops, finalize.
- Persisted-state tests cover event persistence, projection consistency, idempotency completion, replay zero-write behavior, rollback behavior, and transfer-scoped boundary access.
- Architecture guards were updated so transferInventory is the only movement entrypoint allowed to use the private transfer runner instead of the generic `runInventoryCommand` wrapper.

Known limitations:
- Existing broader-suite failures remain outside this boundary refactor: `lint:inventory-writes` reports pre-existing ownership violations, and `test:scenarios` previously failed on non-transfer load/timeout cases.
